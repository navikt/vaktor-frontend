import type { NextPage } from "next";

        import Tidslinje from "../components/tidslinje";

        const Home: NextPage = () => {
        return (
<div>
  <Tidslinje />
</div>
        );
        };

        export default Home;
