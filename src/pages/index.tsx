import type { NextPage } from "next";
import VaktorTimeline from "../components/VaktorTimeline";

const Home: NextPage = () => {
  return (
    <div>
      <VaktorTimeline />
    </div>
  );
};

export default Home;
